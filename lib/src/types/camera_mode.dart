/// Defines the available camera modes.
enum CameraMode {
  /// Only photo capture is available.
  photoOnly,

  /// Only video recording is available.
  videoOnly,

  /// Both photo capture and video recording are available.
  both,
}
